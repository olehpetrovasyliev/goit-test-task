import CardsList from 'components/CadsList/CardsList';
import React from 'react';
import { fetchUsers } from 'services/api';

const Tweets = () => {
  // return <CardsList arr={() => fetchUsers(1)} />;
};

export default Tweets;
